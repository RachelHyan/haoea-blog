# 设置当前目录为根路径
root = true

# [*] 对所有文件生效
[*]
# 结尾是采用 LF(Linux) 还是采用 CRLF(Windows)换行符
end_of_line = lf
# 是否在文件结尾插入空行
insert_final_newline = true
# 设置文件编码
charset = utf-8
# 缩进风格是采用 tab 还是 space
indent_style = tab
# tab缩进大小
tab_width = 2
